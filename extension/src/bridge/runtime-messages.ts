import { BRIDGE_MESSAGE_SOURCE } from './constants'

/** Content → page: background loader finished for a scriptKey. */
export const RUNTIME_PRESET_READY_MESSAGE_TYPE = 'vws-runtime-preset-ready'

/** Content → page: background loader failed for a scriptKey. */
export const RUNTIME_LOAD_FAILED_MESSAGE_TYPE = 'vws-runtime-load-failed'

export { BRIDGE_MESSAGE_SOURCE }
